# nslookup wrapper

Wrapper for
[`curl::nslookup`](https://jeroen.r-universe.dev/curl/reference/nslookup.html)
that returns logical without error.

## Usage

``` r
has_internet_2(host)
```

## Arguments

- host:

  hostname

## Value

logical value
